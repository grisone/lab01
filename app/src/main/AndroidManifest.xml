<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.testaplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_lab"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_lab_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SegundaPantalla"></activity>
        <activity android:name=".AboutActivity"></activity>

        <activity android:name=".tarea4.ListaActivity"></activity>
        <activity android:name=".tarea4.DetalleActivity"></activity>

        <activity android:name=".tarea5.ContactListActivity"></activity>
        <activity android:name=".tarea5.AgregarContactoActivity"></activity>
    </application>

</manifest>
